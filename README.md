This is repo is to test llvm-test-suite & SPEC CPU every day.

>[!IMPORTANT]
>Make sure you have cloned `llvm-project` & `llvm-test-suite` source repo under `llvm`, like below
```
llvm/
├── cpu2006
├── cpu2017
├── important.txt
├── llvm-project
└── llvm-test-suite
```
